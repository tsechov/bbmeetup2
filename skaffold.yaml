apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/tsechov/bbmeetup2:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/tsechov/bbmeetup2:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: bbmeetup2
        chartPath: charts/bbmeetup2
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/tsechov/bbmeetup2'
          image.tag: '{{.DIGEST_HEX}}'
